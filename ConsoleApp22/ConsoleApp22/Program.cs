namespace ConsoleApp22
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Lists<String> lists = new Lists<String>();
            string obj = new string("orxan");
            string obj1 = new string("kenan");
            lists.Add(obj1);
            lists.Add(obj);
            //lists.find(obj);
            
        }
    }
}
